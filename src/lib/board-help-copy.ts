/** Toolbar help control tooltip. */
export function boardHelpTitle(): string {
  return "Keyboard shortcuts (?)";
}

/** Toolbar help control accessible name. */
export function boardHelpAriaLabel(): string {
  return "Keyboard shortcuts";
}

/** Visible glyph on the help control (matches ? hotkey). */
export function boardHelpButtonGlyph(): string {
  return "?";
}

/** Shortcuts dialog close control accessible name. */
export function boardShortcutsCloseAriaLabel(): string {
  return "Close shortcuts";
}

/** Visible Esc glyph on the shortcuts dialog close control. */
export function boardShortcutsEscLabel(): string {
  return "Esc";
}

/** Shortcuts dialog heading (aria-labelledby target). */
export function boardShortcutsDialogTitle(): string {
  return "Board shortcuts";
}
