/** Intro copy under the Filter by stats panel title. */
export function statFiltersHintCopy(): string {
  return (
    "Set min/max for any column. Σσ ≈50 median — lower max = safer picks. " +
    "Save % accepts 91.5, 91.5%, 91,5, or 0.915."
  );
}

export function statFiltersPanelTitle(): string {
  return "Filter by stats";
}

export function clearAllStatFiltersCopy(): string {
  return "Clear all";
}

/** Tooltip for Clear all in the stats filter panel. */
export function clearAllStatFiltersTitle(): string {
  return "Clear all stat range filters";
}

export function doneStatFiltersCopy(): string {
  return "Done";
}

/** Tooltip for Done in the stats filter panel. */
export function doneStatFiltersTitle(): string {
  return "Close filters (Enter)";
}

export function statsFilterButtonLabel(): string {
  return "Stats";
}

/** Landmark label for the sticky board filters chrome. */
export function boardFiltersRegionLabel(): string {
  return "Board filters";
}
